// ignore_for_file: prefer_const_constructors

import 'package:flutter/material.dart';

class IconsManager {
  static final Icon emailIcon = Icon(
    Icons.email,
    size: 25,
  );
  static final Icon passwordIcon = Icon(
    Icons.lock,
    size: 25,
  );
  static final Icon passwordSuffixIcon = Icon(Icons.visibility, size: 20);
  static final Icon nameIcon = Icon(Icons.person);
  static final Icon number = Icon(Icons.numbers_rounded);
}
